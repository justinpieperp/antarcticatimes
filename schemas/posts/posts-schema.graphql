scalar Date

type Post {
  _id: ID!
  title: String!
  author: User!
  body: String!
  category: Category!
  tags: [Tag]
  createdAt: Date
  updatedAt: Date
}

extend type Query {
  getPosts: [Post]
}

extend type Mutation {
  createPost(
    title: String!
    author: String!
    body: String!
    category: String!
    tags: [String]
  ): Post
}
