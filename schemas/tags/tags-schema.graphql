type Tag {
  _id: ID!
  tag: String!
  posts: [Post]
}

extend type Query {
  getTags: [Tag]
  getTagById(id: ID!): Tag
}

extend type Mutation {
  createTag(tag: String!): Tag
  deleteTag(tag: String!): Tag
  updateTag(tag: String!, updateTo: String!): Tag
}
