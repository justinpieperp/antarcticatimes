type Category {
  _id: ID!
  category: String!
  posts: [Post]
}

type Query {
  getCategories: [Category]
}

type Mutation {
  createCategory(category: String!): Category
  deleteCategory(category: String!): String
  updateCategory(category: String!, updateTo: String!): Category
}
